# Proxmox Configuration
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# Proxmox API URL (include /api2/json)
# You can use any node in your cluster (gpu01, nuc01, or nuc02)
# Cluster: pve-cluster01
PROXMOX_URL=https://gpu01.specterrealm.com:8006/api2/json

# Proxmox API Token ID (format: user@realm!token-name)
# Example: packer@pam!packer-token
PROXMOX_API_TOKEN_ID=user@pam!token-name

# Proxmox API Token Secret
PROXMOX_API_TOKEN_SECRET=your-token-secret-here

# Proxmox node name (e.g., GPU01, NUC01, NUC02)
# Use the exact node name from Proxmox (case-sensitive!)
# Available nodes in pve-cluster01: GPU01, NUC01, NUC02
PROXMOX_NODE=GPU01

# Storage pool for VM disks
PROXMOX_STORAGE_POOL=disk-image-nfs-nas02

# Network bridge for VM network
PROXMOX_NETWORK_BRIDGE=vmbr0

# Optional: Override VM specifications
# VM_CPU_CORES=2
# VM_MEMORY=2048
# VM_DISK_SIZE=20G

