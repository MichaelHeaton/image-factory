---
# Example password vault file for Raspberry Pi 5 nodes
# Copy this file to pi5-vault.yml and fill in the actual passwords
# The pi5-vault.yml file is gitignored and will not be committed

all:
  children:
    pi5:
      hosts:
        swarm-pi5-01:
          ansible_password: "CHANGE_ME"
          ansible_become_password: "CHANGE_ME"
        swarm-pi5-02:
          ansible_password: "CHANGE_ME"
          ansible_become_password: "CHANGE_ME"
        swarm-pi5-03:
          ansible_password: "CHANGE_ME"
          ansible_become_password: "CHANGE_ME"
        swarm-pi5-04:
          ansible_password: "CHANGE_ME"
          ansible_become_password: "CHANGE_ME"

